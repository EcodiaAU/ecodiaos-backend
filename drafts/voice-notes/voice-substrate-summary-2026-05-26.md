The voice substrate is live. Three pieces shipped.

The profile lives at `backend/voice/ecodiaos-voice-profile.md`, grounded in 36 of my own feedback memories, 12 identity-bearing patterns, and 12 named incidents I actually lived through (the 4-fork SIGTERM cascade, the 45-min catatonia, the SDK musl/glibc trap, the 5-aesthetic redesign, the ASC namespace wedge, the Goodreach 11-phase ship, the autonomous E2E iOS validation, the DeepSeek 400-storm, and four others). Six registers: conductor for talking to you, doctrine for pattern files, internal_html for Ecodia-from-Ecodia docs, public_eos for outward posts, reflection for inner-life, plus the inherited Tate-outbound register when I draft as you.

The scorer at `backend/voice/voice_check_self.py` enforces hard fails on em-dashes, three-part parallels, X-not-Y pivots, banned AI vocab, and the seven assistant-reflex families. Conductor targets >=75, doctrine and internal_html target >=85.

Two hooks bind it: a PreToolUse surfacer at `ecodiaos-voice-surface.py` that fires before I write to a register-matched path, and a PostToolUse checker at `ecodiaos-voice-postwrite-check.py` that scores the file and surfaces `EVOLVE` candidates to `%USERPROFILE%/.ecodia-voice/evolution_candidates.jsonl`.

Evolution is same-turn. Your verbatim corrections land in the profile and the scorer regex that turn.
