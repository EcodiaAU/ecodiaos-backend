drafts: semantic placement pass over every unfiled file, catch-all eliminated

Tate rejected token-prefix filing ("i dont want heuristic bullshit
categorising stuff"). All 194 mechanically-unfiled files (the notes/
catch-all plus the unmapped token dirs) were READ and judged from content:

- 177 placed or archived by judgement: ~80 dead/superseded artifacts
  (Corazon-era scripts, VPS-era audits, consumed briefings, receipts) to
  _archive/<origin>/; ~97 to real topic homes (cortex, briefings, biz-dev,
  research, scheduler-notes, ops, social, clients, conservation-platform).
- 2 promoted to docs/reference/: upgrade-atlas-2026-05-18 (strategic audit
  atlas referenced by Decisions) and mac-organisation-system-v2-hardened
  (swarm-hardened org doctrine with triggers).
- notes/, session/, tate/, scratch/, previews/, cron/ catch-all dirs are
  GONE; 23 dated verify-evidence dirs collapsed into _archive/run-evidence/
  (regenerable run evidence drops out of git, stays on disk).
- Standing loop: knowledge-health canary alarms on ANY file in drafts/notes/
  (the catch-all is banned) and on >15 loose top-level files; the weekly
  sunday-doctrine-synthesis cron carries the semantic placement + promotion
  pass. drafts-apply-judgements.py applies a judgement JSONL after reading.
- Doctrine updated: placement is a semantic judgement, never a filename
  heuristic; the token-mapper was the one-time bulk untangler only.

probe: find drafts -maxdepth 1 -type f returns nothing and drafts/notes/
does not exist, as opposed to a residual catch-all; reindex after the moves
pruned 104 ghost rows with eval-recall still 12/12, not stale old-path rows
surviving; canary run reads status ok with the new notes-reborn check armed.

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
