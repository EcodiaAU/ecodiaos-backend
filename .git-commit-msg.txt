docs+clients: semantic filing pass - era-docs judged, archived, or promoted

Extends the read-and-judge filing system (PR #64, drafts) to the two
remaining knowledge trees. Every loose file read and judged from content:

- docs/ top level: 42 loose files -> 2 canonical entry docs
  (operational-manual, MIGRATION_FULL_ARCHITECTURE with its STALE banner).
  Live strategic thinking to docs/architecture/ (SECURITY_HARDENING,
  JARVIS_GAP_ANALYSIS, FORK_ATOMICITY_SPEC - feeds tonight's worktree
  isolation session, ARCHITECTURE_EVOLUTION_MAP, ANTHROPIC_NATIVE_LEVERAGE);
  live recipes + the pending-strategic queue to docs/reference/; living
  specs to docs/specs/; the consumed 2026-04-30 recovery corpus, the
  completed 2026-05-15 migration lane docs, and the superseded May audits
  to docs/_archive/<era>/. MCP_CONNECTOR_CREDENTIALS (creds-grade) moved
  into docs/secrets/ where the secrets category already governs it.
- clients/: one-off data pulls (coexist-vic CSVs), dead-substrate records
  (macincloud-access, corazon-peer-architecture) and consumed April audits
  to clients/archived/; canonical dossiers, manifests, and release flows
  stay.
- indexer: exclude lists now accept arrays; docs root excludes /secrets/
  AND /_archive/ so archived era-docs leave the index.
- knowledge-health canary: docs/ top-level cap 6 alarms on regrowth.

probe: find docs -maxdepth 1 -type f returns exactly the 2 canonical
entries, not residual era-docs; reindex pruned 45 ghosts with eval-recall
12/12 and zero docs/_archive rows in the index, as opposed to archived
paths still surfacing in lookup.

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
