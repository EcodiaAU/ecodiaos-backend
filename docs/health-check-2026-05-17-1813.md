# System Health Check - 18:13 AEST Sun 17 May 2026

**Routine:** system-health (4h cadence)
**Session:** cse_01HSkTPrsaP1XWhconhKk4sz (CCR cloud container)
**Outcome:** PARTIAL - VPS alive, MCP OAuth blocked substrate checks

---

## Step 1 - Substrate Aliveness

| Substrate | Status | Detail |
|---|---|---|
| VPS API (/api/health) | **OK** | `{"status":"ok"}` |
| VPS Process (/api/healthz) | **OK** | pid=2417718, uptime=93231s (~25.9h), loop alive |
| Postgres (status_board.query) | **BLOCKED** | MCP OAuth not configured in CCR session |
| Neo4j (node count) | **BLOCKED** | MCP OAuth not configured in CCR session |
| kv_store.get | **BLOCKED** | MCP OAuth not configured in CCR session |

**Root cause for BLOCKED items:** Both `ecodia-core` and `ecodia-shell` MCP servers report `mcp_unauthorized_no_token` on any `tools/call` attempt. The session ingress token authenticates for `tools/list` but Anthropic's CCR proxy requires a separate OAuth token for `tools/call` - that token is not configured in this CCR session.

Error: `{"type":"authentication_error","message":"MCP server requires authentication but no OAuth token is configured.","details":{"error_code":"mcp_unauthorized_no_token"}}`

---

## Step 2 - Recent Error Surface

NOT COMPLETED - status_board P1/P2 query blocked (MCP OAuth unavailable).

---

## Step 3 - Migration Health (2026-05-15+ window)

NOT COMPLETED - status_board row `580f7aaf-d0c5-4153-b712-0b5d6738d3d5` not queryable.

---

## Step 4 - Scheduler Cron Monitoring

NOT COMPLETED - scheduler.list blocked (MCP OAuth unavailable).

---

## Step 5 - Episode Write

NOT COMPLETED - neo4j.write_episode blocked (MCP OAuth unavailable).

---

## Alert Status

- sms.tate: NOT SENT (ecodia-core MCP unavailable)
- Consecutive failures counter: NOT INCREMENTED (kv_store unavailable)

---

## What IS confirmed healthy

- VPS Express process is alive (uptime ~25.9h, PID 2417718 stable)
- MCP tool surface INTACT: 14 tools at /api/mcp/ecodia-core, 4 at /api/mcp/ecodia-shell, 30 at /api/mcp/cowork (tools/list works without auth per MCP spec)
- API /api/health: {"status":"ok"}

---

## Action Required

**Tate:** The CCR session running this system-health routine does not have MCP OAuth configured for `tools/call`. This prevents all substrate checks. Two possible fixes:

1. System health routines should run via the VPS conductor (scheduler POST to /api/os-session/message) where MCP access is already provisioned - not as standalone CCR sessions with fresh OAuth requirements.
2. OR: Configure the OAuth token for ecodia-core and ecodia-shell in the CCR session settings.

This is NOT a VPS infrastructure failure - the API process is healthy. It IS a tooling-access failure for this execution context.
